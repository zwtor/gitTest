package cn.kxy.examination.business;

import com.alibaba.fastjson.JSONObject;
import com.lazy.common.utils.FilePath;
import org.apache.commons.io.FileUtils;

import java.io.File;

public class NewExamination {
//    private static final JSONObject newExamURLObject = JSONObject.parseObject(FileUtils.readFileToString(new File(FilePath.getURLFilePath("new-exam-url.json"))));
}
